name: iOS package

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "lib/**"
      - "assets/**"
      - "pubspec.yaml"
      - "ios/**"
      - ".github/workflows/ios.yml"

permissions:
  contents: write

jobs:
  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Get dependencies
        run: flutter pub get

      - name: Patch llm hook (skip iOS native — brak prebuilt)
        run: |
          chmod +x scripts/patch_llm_hook_skip_ios_ci.sh
          ./scripts/patch_llm_hook_skip_ios_ci.sh
          HOOK=$(find ~/.pub-cache/hosted/pub.dev -path '*/llm_llamacpp-*/hook/build.dart' | head -1)
          grep -q 'pomijam budowę llama.cpp' "$HOOK"

      - name: Build iOS (unsigned via xcodebuild)
        run: |
          flutter build ios --config-only --release --no-codesign
          set -o pipefail
          xcodebuild \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build/ios/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY=- \
            DEVELOPMENT_TEAM= \
            PROVISIONING_PROFILE_SPECIFIER= \
            build 2>&1 | tee /tmp/xcodebuild.log
          APP=$(find build/ios/DerivedData/Build/Products -name 'Runner.app' -type d | head -1)
          if [[ -z "$APP" || ! -d "$APP" ]]; then
            echo "::error::Brak Runner.app — fragment logu:"
            grep -iE 'error:|fatal|CODE_SIGN|provision|Signing for' /tmp/xcodebuild.log | tail -n 80 || true
            tail -n 80 /tmp/xcodebuild.log
            exit 1
          fi
          mkdir -p build/ios/iphoneos
          rm -rf build/ios/iphoneos/Runner.app
          cp -R "$APP" build/ios/iphoneos/Runner.app

      - name: Pack ZIP
        run: |
          mkdir -p dist/Dialectium-iOS
          cp -R build/ios/iphoneos/Runner.app dist/Dialectium-iOS/
          cat > dist/Dialectium-iOS/CZYTAJ-MNIE.txt <<'EOF'
          Dialectium — iOS (bez podpisu Apple)

          To jest Runner.app zbudowany bez codesign (GitHub nie ma konta Apple).

          Na iPhonie NIE zainstalujesz tego jak APK na Androidzie.
          Potrzebny jest Mac + Xcode (albo Apple Developer) żeby podpisać
          aplikację i wgrać na telefon (Xcode / TestFlight).

          1. Rozpakuj ZIP na Macu
          2. Otwórz projekt w Xcode (folder ios/ w repo) albo podpisz Runner.app
          3. Wgraj na iPhone przez kabel / TestFlight
          EOF
          cd dist
          ditto -c -k --sequesterRsrc --keepParent Dialectium-iOS Dialectium-iOS.zip

      - uses: actions/upload-artifact@v6
        with:
          name: Dialectium-iOS
          path: dist/Dialectium-iOS.zip

      - name: Attach to GitHub Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ios-unsigned
          name: Dialectium — iOS
          body: |
            Paczka iOS (**bez podpisu Apple**).

            1. Pobierz **Dialectium-iOS.zip**
            2. Rozpakuj na **Macu**
            3. Podpisz w Xcode / Apple Developer i wgraj na iPhone

            ⚠️ Na telefonie nie zainstalujesz tego jednym kliknięciem jak APK —
            Apple wymaga podpisu (konto deweloperskie lub Xcode + kabel).
          files: dist/Dialectium-iOS.zip
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
