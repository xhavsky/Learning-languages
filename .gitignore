# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
*.iml

# Large Piper ONNX models (download via generate_tts / README)
tools/voices/*.onnx
tools/voices/*.onnx.json

# Lokalne LLM (fetch_ondevice_models.sh) — za duże na git
models/*.gguf
models/*.partial
bundled/ollama/ollama
bundled/ollama/ollama.exe
android/app/src/main/assets/models/*.gguf
!models/README.md

# Release artifacts (build locally)
dist/

# Screenshoty UI — tylko Discord / artefakt CI, nigdy w git
docs/screenshots/

# IDE / lokalne narzędzia — nie na publiczny GH
.idea/
.vscode/
.cursor/

# Portal / collab tooling — lokalnie, poza publicznym GH
anielka-portal/
COLLAB.md
scripts/start-anielka-portal.sh

# Misc
*.log
.flutter-plugins-dependencies

scripts/.trellis_mascot_jobs.json
