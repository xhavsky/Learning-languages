# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
*.iml

# Large Piper ONNX models (download via generate_tts / README)
tools/voices/*.onnx
tools/voices/*.onnx.json

# Lokalne LLM (fetch_ondevice_models.sh) — za duże na git
models/*.gguf
models/*.partial
bundled/ollama/ollama
bundled/ollama/ollama.exe
android/app/src/main/assets/models/*.gguf
!models/README.md

# Release artifacts (build locally)
dist/

# IDE / lokalne narzędzia — nie na GitHub
.idea/
.vscode/
.cursor/

# Osobny projekt portalu — lokalnie OK, nie w tym repo
anielka-portal/

# Misc
*.log
.flutter-plugins-dependencies

scripts/.trellis_mascot_jobs.json
